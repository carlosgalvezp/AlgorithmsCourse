#include <iostream>

int main(int argc, char** argv)
{
    // Declare variables
    int a = 0;
    int b = 0;
    int sum = 0;

    // Read input
    std::cin >> a;
    std::cin >> b;

    // Compute and print output
    sum = a + b;
    std::cout << sum;
    return 0;
}
